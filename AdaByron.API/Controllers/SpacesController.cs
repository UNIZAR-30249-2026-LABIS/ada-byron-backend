namespace AdaByron.Application.Controllers;

// TODO: SpacesController — expone espacios y ocupación del edificio
// GET /api/spaces             → Listar espacios disponibles
// GET /api/spaces/{id}        → Detalle con geometría GeoJSON (PostGIS)
// GET /api/spaces/occupancy   → % ocupación actual del edificio
