namespace AdaByron.Domain.Factories;

// TODO: ReservationFactory — crea y reconstituye objetos Reservation complejos
// Métodos esperados:
//   Create(requesterEmail, spaceId, start, end, attendeeCount) → Reservation (nuevo)
//   Reconstitute(id, ..., status, createdAt, invalidSince) → Reservation (desde BD)
